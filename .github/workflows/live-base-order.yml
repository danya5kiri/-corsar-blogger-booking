name: Check live base order

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  order:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Show compact live base order
        run: node scripts/audit-live-base.mjs
